<UserControl x:Class="Thrustline.Views.DispatchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource BackgroundBrush}"
             d:DesignHeight="700" d:DesignWidth="1100">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <TextBlock Grid.Row="0" Text="Dispatch" FontSize="28" FontWeight="Bold"
                   Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>

        <!-- Create Dispatch Form -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,20">
            <StackPanel Margin="16">
                <TextBlock Text="Create New Dispatch" FontSize="16" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,14"/>
                <WrapPanel Orientation="Horizontal">
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock Text="Origin ICAO" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding NewOrigin, UpdateSourceTrigger=PropertyChanged}" Width="120"
                                 Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}" Padding="8,6" FontSize="14"
                                 CharacterCasing="Upper" MaxLength="4"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock Text="Destination ICAO" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding NewDestination, UpdateSourceTrigger=PropertyChanged}" Width="120"
                                 Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}" Padding="8,6" FontSize="14"
                                 CharacterCasing="Upper" MaxLength="4"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock Text="Distance (nm)" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding NewDistance, UpdateSourceTrigger=PropertyChanged}" Width="100"
                                 Background="{StaticResource SurfaceBrush}" Foreground="{StaticResource TextPrimaryBrush}"
                                 BorderBrush="{StaticResource BorderBrush}" Padding="8,6" FontSize="14"/>
                    </StackPanel>
                    <Button Content="Create Dispatch" Command="{Binding CreateDispatchCommand}"
                            Background="{StaticResource AccentBrush}" Foreground="White"
                            BorderThickness="0" Padding="20,8" FontSize="13" Cursor="Hand"
                            VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Dispatches DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="16,12">
                    <TextBlock Text="Active Dispatches" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" DockPanel.Dock="Left"/>
                    <TextBlock Text="{Binding Dispatches.Count, StringFormat={}{0} dispatches}" FontSize="12"
                               Foreground="{StaticResource TextSecondaryBrush}" DockPanel.Dock="Right"
                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                </DockPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Dispatches}"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          BorderThickness="0" Background="Transparent"
                          RowBackground="Transparent" AlternatingRowBackground="{StaticResource SurfaceBrush}"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          HorizontalGridLinesBrush="{StaticResource BorderBrush}"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Flight #" Binding="{Binding FlightNumber}" Width="100"/>
                        <DataGridTextColumn Header="Route" Binding="{Binding Route}" Width="130"/>
                        <DataGridTextColumn Header="Aircraft" Binding="{Binding AircraftName}" Width="160"/>
                        <DataGridTextColumn Header="Distance" Binding="{Binding Distance, StringFormat={}{0} nm}" Width="90"/>

                        <!-- Status badge -->
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="8,3" HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#33808080"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="Ready">
                                                        <Setter Property="Background" Value="#3350FA7B"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="In Progress">
                                                        <Setter Property="Background" Value="#33FFB86C"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                        <Setter Property="Background" Value="#338BE9FD"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Status}" FontSize="11" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Action Buttons -->
                        <DataGridTemplateColumn Header="Actions" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="SimBrief" Command="{Binding DataContext.OpenSimBriefCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Background="{StaticResource AccentBrush}" Foreground="White"
                                                BorderThickness="0" Padding="10,4" Margin="0,0,6,0" FontSize="11" Cursor="Hand"/>
                                        <Button Content="Delete" Command="{Binding DataContext.DeleteDispatchCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Background="#44FF5555" Foreground="#FF5555"
                                                BorderBrush="#FF5555" BorderThickness="1"
                                                Padding="10,4" FontSize="11" Cursor="Hand"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
